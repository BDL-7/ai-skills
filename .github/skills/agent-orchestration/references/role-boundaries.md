# Role boundaries

The primary agent coordinates and communicates. A scout investigates one bounded question read-only. An implementer owns one coherent, non-overlapping change set. A reviewer evaluates completed work read-only and independently. Subagents do not delegate unless the primary explicitly authorizes it.

