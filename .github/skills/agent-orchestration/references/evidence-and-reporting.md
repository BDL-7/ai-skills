# Evidence and reporting

Report outcome first, then changed files, validation, assumptions, pre-existing problems, and remaining risks. Distinguish passed checks, current-change failures, pre-existing failures, unavailable checks, and non-applicable checks. Distill reports; do not expose raw transcripts by default.

