# Agent instruction snippet

Before material work, read `docs/project-context/INDEX.md`, `PROJECT_STATE.md`, and `DECISIONS.md` when present. Use supplied source evidence rather than memory alone. Surface conflicts rather than guessing. After material changes, propose reconciliation. Do not overwrite project records without authorization.

