# Framework-enabled example

When repository paths show BMAD, OpenSpec, or Spec Kit artifacts, list their locations and source role in `FRAMEWORK_INTEGRATION.md`. Keep the framework records authoritative in their own scope; reconcile project context after change implementation.

